import { getAllUsers, makeSignIn, makeSignUp } from '../getServerData';

const service = {
    getAllUsers: () => {
        
    },

    makeSignIn: () => {

    },

    makeSignUp: () => {
        
    }
}

export default service;